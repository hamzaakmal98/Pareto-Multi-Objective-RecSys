
[project]
name = "kuairand-like-prediction"
version = "0.1.0"
description = "Prototype repo for predicting the `is_like` label in KuaiRand-style data."
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
