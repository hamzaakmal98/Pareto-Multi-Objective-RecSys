# Experiments

Experiment configuration and reproducibility notes.

Use YAML config files in `configs/` (example configs to be added) and central trainer to drive runs.

Save model artifacts to `artifacts/models` and checkpoints to `artifacts/checkpoints`.
